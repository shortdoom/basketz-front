
// Add a section to put amount with token type in front, this creates an arrray of address quantity
// Add a section with badges and amount of token with code or picture and remove icon to show the token being wrapped
// perform the wrapper flow which make the deposit button in loading state until the flow is finished
export default function WrapperCard() {
}